version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_DB: MathELearning
      POSTGRES_USER: elearning_user
      POSTGRES_PASSWORD: 123
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: myapi
    container_name: myapi_container
    depends_on:
      - postgres
    ports:
      - "80:8080"
    env_file:
      - .env
    restart: always

  # Core AI service (Gemini / core processing) — internal only, via the compose network (api -> ai_core:5001).
  ai_core:
    build:
      context: ./AI
      dockerfile: Dockerfile
    container_name: ai_core
    restart: always
    expose:
      - "5001"
    ports:
      # Bind to localhost only for debugging; do NOT publish externally.
      - "127.0.0.1:5001:5001"
    env_file:
      - .env
      - AI/.env


volumes:
  pgdata:
